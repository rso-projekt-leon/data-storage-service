apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-storage-deployment
  labels:
    app: data-storage
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: data-storage
  template:
    metadata:
      labels:
        app: data-storage
    spec:
      containers: 
      - image: leon11sj/data-storage-microservice
        name: data-storage
        command: ["gunicorn"]
        args: ["--bind", "0.0.0.0:5000", "manage:app"]
        env:
        - name: FLASK_ENV
          value: "production"
        - name: APP_SETTINGS
          value: "app.config.ProductionConfig"
        - name: FLASK_APP
          value: app/__init__.py
        - name: AWS_DEFAULT_REGION
          valueFrom:
            secretKeyRef:
              name: aws-region
              key: AWS_DEFAULT_REGION
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-secret-access-key
              key: AWS_SECRET_ACCESS_KEY
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws-access-key-id
              key: AWS_ACCESS_KEY_ID      
        ports:
        - containerPort: 5000
          name: server
          protocol: TCP
---

apiVersion: v1
kind: Service
metadata:
  name: data-storage
spec:
  type: LoadBalancer
  ports:
  - port: 8082
    protocol: TCP
    targetPort: server
  selector:
    app: data-storage
